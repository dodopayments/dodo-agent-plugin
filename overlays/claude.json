{
    "//": "Claude Code extras merged over the canonical plugin.json by scripts/build.mjs. Agent Plugins v1 has no portable secrets mechanism (deliberately deferred by the spec), so userConfig lives here and is emitted only into .claude-plugin/plugin.json.",
    "userConfig": {
        "dodo_api_key": {
            "type": "string",
            "title": "Dodo Payments API Key",
            "description": "Optional. Only required if you switch the API MCP server in .mcp.json to local stdio mode (dodopayments-mcp). The default remote SSE server authenticates via OAuth in your browser.",
            "sensitive": true
        },
        "dodo_webhook_key": {
            "type": "string",
            "title": "Dodo Payments Webhook Key",
            "description": "Optional. Only required for local stdio MCP mode. Obtain from Dashboard > Developer > Webhooks.",
            "sensitive": true
        },
        "dodo_environment": {
            "type": "string",
            "title": "Environment",
            "description": "Either test_mode or live_mode. Only used by the local stdio MCP server.",
            "sensitive": false
        }
    }
}
